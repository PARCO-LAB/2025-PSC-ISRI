#include <stdio.h>

int main() {
    // Dichiarazione
    int a = 25, b = 2, c;
    float e = 25.0, f = 2.0, g;

    // Operazioni
    c = a / b;
    g = e / f;

    // Stampa
    printf("interi: %i\n", c);
    printf("float : %f\n", g);
return 0;
}