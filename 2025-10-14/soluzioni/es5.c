#include <stdio.h>

int main(){
    
    // Dichiarazione e assegnamento
    float A = 12.3;
    float B = 15.5321;
    float C = -93.0;
    float D = 12746.0372;
    
    // Stampo i risultati
    printf("%f + %f = %f\n", A, B, A + B);
    printf("%f - %f = %f\n", D, C, D - C);

    return 0;
}